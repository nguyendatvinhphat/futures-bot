{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:$PORT dashboard.app:app"
  }
}
